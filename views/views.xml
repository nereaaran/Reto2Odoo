<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="libros.list">
      <field name="name">libros list</field>
      <field name="model">libros.libros</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="libros.action_window">
      <field name="name">libros window</field>
      <field name="res_model">libros.libros</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="libros.action_server">
      <field name="name">libros server</field>
      <field name="model_id" ref="model_libros_libros"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="libros" id="libros.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="libros.menu_1" parent="libros.menu_root"/>
    <menuitem name="Menu 2" id="libros.menu_2" parent="libros.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="libros.menu_1_list" parent="libros.menu_1"
              action="libros.action_window"/>
    <menuitem name="Server to list" id="libros" parent="libros.menu_2"
              action="libros.action_server"/>
    -->
  </data>
</odoo>